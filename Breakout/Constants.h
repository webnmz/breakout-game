#pragma once
// 窗口大小常量
const int WINDOW_WIDTH = 800;
const int WINDOW_HEIGHT = 600;

// 挡板大小常量
const float PADDLE_WIDTH = 100;
const float PADDLE_HEIGHT = 15;

// 逻辑分辨率
const sf::Vector2f LOGICAL_SIZE(800, 600);
const float LOGIC_WIDTH = 800.f;
const float LOGIC_HEIGHT = 600.f;

const float BALL_RADIUS = 10.f;// 小球半径
const std::string HIGH_SCORE_FILE = "highscore.txt";  // 存储最高分的文件路径